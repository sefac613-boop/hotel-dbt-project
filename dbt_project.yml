name: 'happy_bookings'
version: '1.0.0'
config-version: 2

profile: 'happy_bookings'

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  happy_bookings:
    materialized: view